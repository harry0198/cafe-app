<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CafeApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="backgroundColor">@color/white</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorPrimary">@color/black</item>
        <item name="colorAccent">@color/green</item>
        <item name="colorSecondary">@color/light_grey</item>
        <item name="colorSecondaryContainer">@color/white</item>
        <item name="android:statusBarColor">@color/green</item>
        <item name="android:navigationBarColor">@color/green</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="ToolbarColoredBackArrow" parent="Base.Theme.CafeApplication">
        <item name="android:textColorSecondary">@color/primaryOnBackground</item>
    </style>


    <style name="Theme.CafeApplication" parent="Base.Theme.CafeApplication" />

    <style name="ActionBarWhite" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!-- This code is for changing the color of the bar. -->
        <!-- Type your colour code which you want to set in colorPrimary item -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="ActionBarCaution" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!-- This code is for changing the color of the bar. -->
        <!-- Type your colour code which you want to set in colorPrimary item -->
        <item name="colorPrimary">?attr/colorError</item>
    </style>

    <style name="ActionBarNoBackground" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!-- This code is for changing the color of the bar. -->
        <!-- Type your colour code which you want to set in colorPrimary item -->
        <item name="colorPrimary">@color/transparent</item>
    </style>

    <style name="ButtonGreen" parent="Theme.CafeApplication">
        <item name="colorPrimary">@color/green</item>
    </style>

    <style name="ButtonNoBackground" parent="Theme.CafeApplication">
        <item name="colorPrimary">@color/transparent</item>
    </style>
</resources>